var fruit = ['Apple', 'Orange', 'Grapefruit'];
// indexOf returns the index position of an item in an array
// if the item isn't in the array, indexOf always returns -1
// so you can use a check against -1 to see if something is in an array
var position = fruit.indexOf('Grapefruit');

console.log(position);
